<%= render 'shared/hamburger' %>
<%= render 'shared/brand' %>


<h1 class="mt-5">MOBILE</h1>



<div class="portfolio-mobile-grid">
  <% @mirrors.each do |mirror|  %>
    <%= render partial: "mirror_mobile_grid", locals: { mirror: mirror } %>
  <% end %>
</div>
