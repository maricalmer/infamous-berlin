<%= render 'shared/navbar' %>
<div class="search-bar bck-btn-reload-js users-index-js">
  <%= simple_form_for :search, url: users_path, html: { class: "search-bar__form search-form-js" }, remote: true, method: :get do |f| %>
    <div class="search-bar__input-wrapper search-data-js" data-set="<%= @autocomplete_set.to_json %>">
      <input type="text" name="query" placeholder="Search through users..." class="search-bar__input search-input-js refresh-input-js user-input-highlight-js" autocomplete="off">
      <button class="search-bar__btn" type="submit">
        <i class="fas fa-search"></i>
      </button>
    </div>
  <% end %>
</div>
<%= render "index_user", users: @users, query: params[:query] %>
